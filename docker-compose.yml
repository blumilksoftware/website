version: "3.7"

services:
  node:
    container_name: blumilk-node
    image: node:15.2.1-alpine3.12
    working_dir: /application
    volumes:
      - .:/application
    restart: always
    environment:
      - HOST=0.0.0.0
