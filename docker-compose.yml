version: "3.7"

services:
  node:
    container_name: blumilk-node
    image: node:alpine3.11
    working_dir: /application
    volumes:
      - .:/application
    restart: always
    environment:
      - HOST=0.0.0.0
